# Gallery content is generated by the outer repo's CI pipeline
# (`.github/scripts/fetch-galleries.py` in steam228/classes) and
# written into this submodule's working dir on every build.
# It does not belong in this submodule's git history.
Galeria/

.DS_Store
